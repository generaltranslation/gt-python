[project]
name = "flask-lazy-example"
version = "0.1.0"
description = "Flask example with lazy translation loading"
requires-python = ">=3.10"
dependencies = [
    "flask>=3.0.0",
    "httpx>=0.27.0",
    "gt-flask",
]

[tool.uv]
package = false

[tool.uv.sources]
gt-flask = { workspace = true }
